<h3>AddRawCharsetData([ width ], [ height ] )</h3>
Adds the raw charset data of an image to the internal charset data set. Useful for
saving full-frame NxN images.
<h4>Example</h4>
Assume you have a 16x32 multicolor image (4x4 block of characters). You can then add frames
and finally save the raw data s such:
<code>
... renderframe
AddRawCharsetData(4,4)
... renderframe
AddRawCharsetData(4,4)
... renderframe
AddRawCharsetData(4,4)
...
SaveRawData("data/N_frames_of_4x4_char_data.bin")
</code>
