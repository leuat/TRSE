Saves the accumulated raw data in the data store (image, sprite data etc) to disk, and then clears the data.
<code>
-- Save raw data (such as an image, sprite, screen) and delete
SaveDraw("data/myFile.bin");
</code>
