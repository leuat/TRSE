<p>Gets the low bit of an address or zero page pointer. 
Example:  
<code>
var
   data : incbin("myData.bin", $2050);
   i:byte;
   zp: pointer;
begin
	zp:=data;
	i:=hi(data); // will be $20
	i:=hi(zp); // will be $20
	i:=lo(data); // will be $50
	i:=lo(zp); // will be $50
end;

</code>