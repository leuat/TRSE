<h3>Min ( a,b );</h3>
<p>
    Returns the minimum of a and b. "b" must be a variable or a constant expression.

<p>
Example:
<code>
j:=min(j+1,100); // increases j from 100 to 255 but no more
</code>

