<h3>Description</h3>
<p>The for to do loop is a repetition structure that will count between two values.</p>
<p>Step allows the counter to increment by a number other than 1.</p>


<h3>Example 1</h3>

<code>
for i := 1 to 10 do
begin
    // loops 10 times
end;
</code>

<h3>Example 2</h3>

<code>
for index := 2 to 10 step 2 do
begin
    // counts from 2 to 10 in steps of 2, looping 5 times
end;
</code>
