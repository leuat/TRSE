program HelloWorldOric;
@use "system/screen"
var

	@export "resources/charmaps/charmap1.flf" "resources/charmaps/charmap1.bin" 256
	
	charmap: incbin("resources/charmaps/charmap1.bin");

	src, dst: ^byte;
	i:byte;

begin
	Screen::PrintText("Hello world TRSE", 5, 10, 1);
//	Screen::PrintText("ABCDEFGGHIJKLMNOPQRSTUVWZYS", 5, 10, %00000001);
//	blockmemcpy(#charmap,#Screen::charmap,4);
/*
	src:=#charmap;
	dst:=$B400+64*8;
	for i:=0 to 4 do
	begin
		memcpy(src, 0, dst, 0);
		asm(" inc src+1");
		asm(" inc dst+1");
	end;*/
	loop();
end.