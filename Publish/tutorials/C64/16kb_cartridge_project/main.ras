program CRTProject;
var  
   index: byte; 
 // This example file MUST be exomized for the cartridge to work!
begin
	clearscreen(key_space,screen_char_loc);
	clearscreen(LIGHT_GREEN,screen_col_loc);
	moveto(4,11,hi(screen_char_loc));
	printstring("THIS IS A CARTRIDGE PROJECT",0,40);
	moveto(0,13,hi(screen_char_loc));
	printstring("COLOR AND MEMORY MAP IN PROJECT SETTINGS",0,40);
	
	Loop();
end.
