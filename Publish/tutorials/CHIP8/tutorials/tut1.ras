program Tut1;
@use "system/system"


@export "resources/charsets/charset1.flf" "resources/charsets/charset1.bin" 26
@bin2inc "resources/charsets/charset1.bin" "charset.inc" chars
var
	x,y,z : byte;
	
@include "charset.inc"
	
begin
	System::Clear();
	System::Beep(100);

	for x:=0 to 10 do
	begin
		asm(";here");
		z:=System::Random();
		System::DrawLine(z,x,%10000001);
	end;	


	asm("
loop:
	jp loop

	");

	
end.
